import React from 'react';
import {BiWorld} from "react-icons/bi" 
const World = ({props}) => {
    return (
        <div>
            <BiWorld className={props}/>
        </div>
    );
}

export default World;
