import React from 'react'
import { BsInstagram } from 'react-icons/bs'

const Instagram = ({ props }) => {
    return (
        <BsInstagram className={`${props}`} />
    )
}

export default Instagram